# CLAUDE.md

This repository's contributor and agent guidance lives in [AGENTS.md](AGENTS.md).
Read it for setup, testing, conventions, project structure, and PR rules.

## Notes for Claude Code

The runnable code lives only in `demos/acp/acp-node/`. Run every npm command from that directory, not the repository root. Before finishing a change, run `npm test` there and confirm all 35 tests pass.
